import 'package:flutter/material.dart';

class Level01PageMS extends StatelessWidget {
  const Level01PageMS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

import 'package:flutter/material.dart';

class Level01PageLS extends StatelessWidget {
  const Level01PageLS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("homels"),
    );
  }
}

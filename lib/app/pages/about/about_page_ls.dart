import 'package:flutter/material.dart';

class AboutPageLS extends StatelessWidget {
  const AboutPageLS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("About LS")),
    );
  }
}

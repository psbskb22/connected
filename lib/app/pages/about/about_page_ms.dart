import 'package:flutter/material.dart';

class AboutPageMS extends StatelessWidget {
  const AboutPageMS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
